# PPI公示業務取得ツール 仕様書

| 項目 | 内容 |
|---|---|
| システム名称 | PPI公示業務取得ツール |
| 文書版数 | 1.1 |
| 作成日 | 2026年9月4日 |
| 対象プログラム | `ppi_tool.py`、`selection_web.py` |
| 対象OS | Windows 11 |
| Python | 3.11以上 |

## 1. システム概要

### 1.1 目的

国土交通省等が提供する入札情報サービス（i-PPI）に掲載されている「入札公告等（業務）」から、指定日に公告された案件を自動取得する。取得結果をローカルWeb画面に表示し、利用者が選択した案件だけをCSV形式で出力する。

### 1.2 対象サイト

| 項目 | 内容 |
|---|---|
| サイト名 | 入札情報サービス（i-PPI） |
| URL | https://www.i-ppi.jp/ |
| 対象メニュー | 業務検索 > 入札公告等 |
| 検索条件 | 公告日の開始日・終了日ともにWeb画面で指定した日 |

### 1.3 システム構成

本ツールはSeleniumでChromeまたはEdgeを操作し、Beautiful Soupで取得HTMLを解析する。取得したデータはFlaskによるローカルWeb画面で確認・選択し、pandasを用いてCSVへ出力する。

```text
i-PPI
  ↓ Selenium（画面遷移・検索・ページング・案件概要表示）
ppi_tool.py
  ↓ Beautiful Soup（HTML解析）
案件データ
  ↓ 重複排除
selection_web.py（確認・絞り込み・選択）
  ↓ 選択案件
pandas → CSV
```

## 2. 動作環境

### 2.1 必須環境

| 項目 | 要件 |
|---|---|
| OS | Windows 11 |
| Python | 3.11以上 |
| ブラウザ | Google ChromeまたはMicrosoft Edge |
| ネットワーク | HTTPSでi-PPIおよびWebDriver配布元へ接続可能であること |

### 2.2 使用ライブラリ

| ライブラリ | 用途 |
|---|---|
| selenium | Webブラウザ自動操作 |
| webdriver-manager | WebDriver取得の代替手段 |
| beautifulsoup4 | 検索結果・案件概要HTMLの解析 |
| Flask | ローカル案件選択画面 |
| pandas | 一覧データ作成、CSV出力 |

依存バージョンは `requirements.txt` で管理する。

## 3. ファイル構成

| ファイル・フォルダ | 内容 |
|---|---|
| `ppi_tool.py` | メインプログラム |
| `selection_web.py` | 案件選択Webアプリケーション |
| `templates/` | 案件選択・完了画面のHTML |
| `PPI公示業務Web起動.vbs` | コンソール非表示のWindows用ランチャー |
| `config.json` | 動作設定、HTML要素ID、取得項目候補 |
| `requirements.txt` | Python依存ライブラリ |
| `README_PPI.md` | インストールおよび操作手順 |
| `test_ppi_tool.py` | 単体テスト |
| `output/` | 選択済みCSV出力先。実行時に自動作成 |
| `logs/` | ログ出力先。実行時に自動作成 |

## 4. 機能仕様

### 4.1 当日公告案件検索

1. ローカルWeb画面を開き、利用者が公示日を指定する。
2. i-PPIトップページへアクセスする。
3. `contents` iframe内の「業務検索 > 入札公告等」を開く。
4. 公告日の開始日と終了日にWeb画面の指定日を設定する。
5. 一覧表示件数を100件に設定する。
6. 検索開始ボタンを押す。
7. 検索結果表が表示されるまで待機する。
8. 取得中または取得エラーをローカルWeb画面へ表示する。

### 4.2 一覧取得

i-PPIの検索結果表 `dgrSearchList` から次の項目を取得する。

- 発注機関／担当部・事務所
- 業務名
- 入札契約方式
- 業務区分
- 公告日

発注機関は「発注機関／担当部・事務所」の `／` より前を採用する。

### 4.3 案件概要取得

`fetch_details` が `true` の場合、各案件の業務名リンクを順番に開き、次の情報を補完する。

| 出力項目 | 案件概要で検索する項目 |
|---|---|
| 地域 | 業務対象地域、履行場所、業務場所、地域 |
| 履行期間 | 履行期間、履行期限、業務期間 |
| 詳細URL | 案件概要に掲載されている公開文書の外部URL |

値が掲載されていない場合は空文字とし、他項目からの推測は行わない。「自： 至：」のように開始・終了とも未設定の地域は空文字として扱う。

i-PPIの案件概要URLはASP.NETのセッションとPostBackに依存し、案件固有の恒久URLではない。そのため、公開文書の外部URLが存在する場合はそのURLを詳細URLとして優先する。外部URLがない場合は、表示中の案件概要URLを設定する。

### 4.4 ページング

1. 現在ページの全案件を取得する。
2. 検索結果表内から次ページのPostBackリンクを検索する。
3. 次ページがあればリンクを押し、結果表の再描画を待つ。
4. 最終ページまたは最大取得件数に達するまで繰り返す。

1ページの表示件数は100件とする。最大取得件数は5,000件とし、`max_records` で1～5,000件の範囲を指定できる。

### 4.5 重複排除

次の複合キーが同一の案件を重複とみなし、最初に取得した1件だけを出力する。

```text
案件名 + 発注機関 + 公示日
```

業種区分、入札方式、地域、履行期間が異なっていても、上記キーが同じ場合は重複として除外する。

### 4.6 Web確認・案件選択

1. 取得した案件を `127.0.0.1` のローカルWeb画面に表示する。
2. 案件名、発注機関、業種区分、地域、入札方式を対象に文字列で絞り込めるようにする。
3. 表示中の案件を一括選択、または全案件を選択解除できるようにする。
4. 各案件の詳細URLを別タブで開けるようにする。
5. CSV出力時は1件以上の選択を必須とし、不正な案件番号を拒否する。
6. 選択画面はループバックアドレスだけで待ち受け、外部ネットワークには公開しない。

### 4.7 CSV出力

| 項目 | 内容 |
|---|---|
| 保存先 | `output/`。設定で変更可能 |
| ファイル名 | `ppi_selected_yyyyMMdd.csv` |
| 文字コード | UTF-8 BOM付き（`utf-8-sig`） |
| 区切り文字 | カンマ |
| ヘッダー | あり |

検索結果が0件の場合は結果画面で0件と表示し、CSVは出力しない。CSV出力後はローカルWebサーバーを停止してプログラムを終了する。

### 4.8 ログ出力

| 項目 | 内容 |
|---|---|
| 保存先 | `logs/`。設定で変更可能 |
| ファイル名 | `ppi_yyyyMMdd.log` |
| 文字コード | UTF-8 |

次の情報をコンソールとログファイルへ出力する。

- 開始日時
- 検索条件
- 接続試行回数
- ページごとの取得件数
- 重複排除後の取得件数
- 異常件数
- 出力ファイルの絶対パス
- 終了日時
- 処理時間

## 5. 出力データ仕様

CSVの列順は次のとおりとする。

| No. | 項目名 | 内容 | 未取得時 |
|---:|---|---|---|
| 1 | 公示日 | i-PPI上の公告日。`yyyy/MM/dd` | 空文字 |
| 2 | 案件名 | 業務名 | 空文字 |
| 3 | 発注機関 | 発注機関名。担当部・事務所を除く | 空文字 |
| 4 | 業種区分 | i-PPI上の業務区分 | 空文字 |
| 5 | 地域 | 業務対象地域等 | 空文字 |
| 6 | 履行期間 | 履行期間または履行期限 | 空文字 |
| 7 | 入札方式 | 入札契約方式 | 空文字 |
| 8 | 詳細URL | 公開文書URL、なければ案件概要URL | 空文字 |

## 6. 設定仕様

設定ファイルはUTF-8のJSON形式とする。

| 設定名 | 型 | 既定値 | 内容 |
|---|---|---|---|
| `site_url` | 文字列 | `https://www.i-ppi.jp/` | i-PPIトップURL |
| `search_url` | 文字列 | 業務公告検索URL | 保守用検索画面URL |
| `search_type` | 文字列 | `業務` | 検索種別。業務のみ許可 |
| `output` | 文字列 | `csv` | `csv`、`excel`、`both` |
| `browser` | 文字列 | `auto` | `auto`、`chrome`、`edge` |
| `headless` | 真偽値 | `true` | ブラウザ画面を非表示にするか |
| `use_webdriver_manager` | 真偽値 | `false` | webdriver-managerを使用するか |
| `max_retry` | 整数 | `3` | 最大試行回数 |
| `retry_wait_seconds` | 数値 | `5` | 再試行前の待機秒数 |
| `wait_timeout_seconds` | 数値 | `30` | 画面要素の最大待機秒数 |
| `page_load_timeout_seconds` | 数値 | `60` | ページ読込タイムアウト秒数 |
| `detail_wait_seconds` | 数値 | `0.2` | 案件詳細間の待機秒数 |
| `max_records` | 整数 | `5000` | 最大取得件数 |
| `fetch_details` | 真偽値 | `true` | 案件概要を巡回するか |
| `output_dir` | 文字列 | `output` | データ出力フォルダ |
| `log_dir` | 文字列 | `logs` | ログ出力フォルダ |
| `csv_encoding` | 文字列 | `utf-8-sig` | CSV文字コード |
| `selection_host` | 文字列 | `127.0.0.1` | 選択画面の待受先。ループバックのみ許可 |
| `selection_port` | 整数 | `8765` | 選択画面の待受ポート。使用中の場合は空きポートを自動選択 |
| `open_selection_browser` | 真偽値 | `true` | 選択画面を既定ブラウザで自動表示するか |
| `selectors` | オブジェクト | 要素ID一式 | i-PPI画面要素ID |
| `field_labels` | オブジェクト | 項目名候補一式 | 案件概要の項目名候補 |

## 7. ブラウザ選択仕様

`browser=auto` の場合は次の優先順位で選択する。

1. Google Chrome
2. Microsoft Edge
3. どちらも存在しない場合はエラー終了

`headless=true` の場合はブラウザ画面を表示しない。`--headed` オプションを指定すると設定にかかわらず画面を表示する。

## 8. リトライ・エラー処理

### 8.1 全体取得リトライ

通信障害、タイムアウト、画面要素操作失敗など、取得処理を継続できない例外が発生した場合はブラウザを終了し、最初から再実行する。

- 最大試行回数: `max_retry`（既定3回）
- 試行間隔: `retry_wait_seconds`（既定5秒）
- 全試行失敗: エラーログを出力し、終了コード1で終了

### 8.2 案件概要解析エラー

1案件の概要取得・解析に失敗した場合は、次の処理を行う。

1. 異常件数を1件加算する。
2. 案件名と例外情報をログ出力する。
3. 一覧画面へ戻る。
4. 次の案件を処理する。

### 8.3 検索結果0件

異常終了せず、選択画面とCSVを作成せずに終了する。

### 8.4 出力エラー

出力フォルダ作成またはCSV書込みで例外が発生した場合はエラーログを出力し、終了コード1で終了する。

## 9. コマンドライン仕様

### 9.1 通常実行

エクスプローラーで `PPI公示業務Web起動.vbs` をダブルクリックする。`pythonw.exe` で起動するため、PowerShellおよびコンソール画面は表示しない。

保守用のコマンドライン実行:

```powershell
python ppi_tool.py
```

### 9.2 オプション

| オプション | 内容 |
|---|---|
| `--config PATH` | 設定ファイルを指定。既定は `config.json` |
| `--browser auto\|chrome\|edge` | 使用ブラウザを上書き |
| `--headed` | ブラウザ画面を表示 |
| `--date YYYY-MM-DD` | 検索する公示日を指定 |
| `--no-open` | 選択画面を自動表示せずURLだけを案内 |

### 9.3 終了コード

| コード | 内容 |
|---:|---|
| 0 | 正常終了 |
| 1 | 設定、取得、解析、出力などの異常終了 |

## 10. 性能仕様

- 最大取得件数: 5,000件
- 一覧表示件数: 1ページ100件
- 目標処理時間: 10分以内

`fetch_details=true` の場合は案件ごとに概要画面との往復通信が必要なため、件数やネットワーク速度によって10分を超える可能性がある。処理時間を優先する運用では `fetch_details=false` とし、一覧項目のみを取得する。この場合、地域、履行期間、詳細URLは空欄となる。

## 11. テスト仕様

`pytest` により次の項目を検証する。

- 検索結果HTMLからの一覧項目抽出
- 検索結果0件
- 想定外HTMLの解析エラー
- 案件概要からの地域・履行期間・公開文書URL抽出
- 未設定地域の空欄化
- 詳細URLの代替処理
- 複合キーによる重複排除
- UTF-8 CSV出力
- Web画面の案件表示とHTMLエスケープ
- 未選択・不正案件番号の拒否
- 選択案件だけのCSV出力
- 設定の既定値マージ
- 不正設定値の拒否

実行コマンド:

```powershell
python -m pytest -q
```

2026年8月20日の実サイト確認では、検索結果27件、重複排除後25件、異常0件でCSV出力に成功した。

## 12. セキュリティ・運用上の注意

- 本ツールは公開情報のみを取得し、i-PPIへのログイン情報は扱わない。
- アクセス頻度を過度に上げないよう、案件概要間に待機時間を設定する。
- 出力データの利用および再配布は、i-PPIおよび各発注機関の利用条件に従う。
- i-PPIのメンテナンス中は取得できない場合があるため、ログを確認して再実行する。
- 選択画面はローカル操作を前提とし、外部公開しない。

## 13. 既知の制約

1. i-PPIのHTML要素IDや画面構成が変更された場合、設定またはプログラム修正が必要となる。
2. 案件概要に地域や履行期間が掲載されていない場合は空欄となる。
3. i-PPIの案件概要画面自体には案件固有の恒久URLがない。
4. 公開文書URLは発注機関のサイト構成変更や掲載終了により参照できなくなる場合がある。
5. 同じ案件名・発注機関・公示日で業種区分だけが異なる案件も、仕様上は重複として1件にまとめられる。
6. 詳細取得を有効にした大量取得では、10分以内の完了を保証できない。

## 14. 将来拡張

- 複数日を対象とした検索
- 前回取得結果との差分抽出
- データベース保存
- メール、Microsoft Teams通知
- Windowsタスクスケジューラ用登録スクリプト
- 案件詳細取得の効率化
- サイト構成変更の自動検知

## 15. 変更履歴

| 版数 | 日付 | 内容 |
|---|---|---|
| 1.0 | 2026-08-20 | 初版作成 |
